/** Atmosphere estático — sem animações de scroll (evita piscar) */
export function initScrollAtmosphere() {
  /* glow fixo em CSS */
}

export function initMouseParallax() {
  /* desativado — parallax no vídeo causava jitter */
}
